$(function(){
    $('.carousel').carousel({
        interval:8000,
       /* pause:"null" *//*pausa a transição se colocar o mouse em cima da foto */
       /*wrap: false*/ /* quando chegar na ultima foto não volta automaticamente para a primeira */
    });
});